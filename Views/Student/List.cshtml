@model List<MVC_Students.Models.Student>

<style>
    table, td {
        border: 1px solid black;
    }
    td {
        padding: 5px;
    } 
</style>
<div style="margin-top: 20px">
    <hr />
    <table cellpadding="10">
        <tr><td>Id</td><td>Name</td><td>Surname</td><td>Group Id</td><td>Status</td><td></td></tr>
        @foreach (var item in MVC_Students.Controllers.StudentController.Students)
        {
            if (item.Id > -1)
            {
                <tr><td>@item.Id</td>
                <td>@item.Name</td>
                <td>@item.Surname</td>
                <td>@item.GroupId</td>
                <td>@item.Status</td>
                <td><a href="@Url.Action("Edit", "Student", new { num = @item.Id })">Редактировать</a></td></tr>
            }
        }
    </table>
</div>